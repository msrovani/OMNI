apiVersion: v1
kind: Service
metadata:
  name: pde-engine
  namespace: omni-grid
spec:
  selector:
    app: pde-engine
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
    - name: http
      port: 8001
      targetPort: 8001
